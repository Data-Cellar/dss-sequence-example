FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements-dss.txt .
RUN pip install --no-cache-dir -r requirements-dss.txt

# Copy application code
COPY dss-api/ .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]